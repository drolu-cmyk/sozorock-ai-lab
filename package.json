{
  "name": "sozorock-ai-lab",
  "version": "2.0.0",
  "private": true,
  "description": "AWS-hosted public website and application intake for SozoRock AI Lab.",
  "license": "MIT",
  "scripts": {
    "serve": "serve site -l 4173",
    "build": "bash scripts/build.sh",
    "generate:legal": "bash scripts/render-legal.sh",
    "generate:cloudformation": "bash scripts/render-cloudformation.sh",
    "test:content": "node scripts/check-content.js",
    "test:seo": "node scripts/check-seo.js",
    "test:accessibility": "node scripts/check-accessibility.js",
    "test:security": "node scripts/check-security.js",
    "pretest": "npm run generate:legal && npm run generate:cloudformation",
    "test": "npm run test:content && npm run test:seo && npm run test:accessibility && npm run test:security && npm run build"
  },
  "devDependencies": { "serve": "^14.2.4" }
}
